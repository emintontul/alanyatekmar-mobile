import React from 'react';

import {AppScreen, Text} from '@/components';

const Invoices = () => {
  return (
    <AppScreen>
      <Text title>Invoice Main Page</Text>
    </AppScreen>
  );
};

export default Invoices;
